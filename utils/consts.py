# path to directory of training data
training_data_path = 'data/training'

# path to directory of testing data
testing_data_path = 'data/testing'

# the amount of error to allow before terminating gradient descent
epsilon = 0.01

# step size used for gradient descent
eta = 1

# regularization penalty
lambda_hyperparameter = 0.00001

# the max number of iterations of gradient descent before it is stopped
max_iterations = 120000

# the directory used to hold trained model files
model_dir = 'models'
